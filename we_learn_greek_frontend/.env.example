# Copy to .env and set VITE_API_URL when the Django backend is hosted.
# Endpoint paths are in src/constants/endpoints.js — only this host changes at deploy time.

VITE_API_URL=http://localhost:8000/api

# Set to false in production when the API is live (disables demo-data fallback).
VITE_ENABLE_DEMO_DATA=true
